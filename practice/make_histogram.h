#ifndef MAKE_HISTOGRAM_H_INCLUDED
#define MAKE_HISTOGRAM_H_INCLUDED
#include <vector>
using namespace std;

vector<size_t> make_histogram(const vector<double>& numbers, size_t bin_count, size_t number_count);

#endif // MAKE_HISTOGRAM_H_INCLUDED
